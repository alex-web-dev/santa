extends layouts/_layout.pug

block vars
	- var selectedLang = 'ru';
	- var headerClass = 'header--black';

block title
	| !{lang.privacyPolicyPageTitle}

block description
	meta(name="description" content=lang.privacyPolicyPageDescription)

block content
	main.main
		.terms-info
			.container
				.terms-info__main !{ lang.privacyPolicyContent }