extends layouts/_layout.pug

block vars
	- var selectedLang = 'ru';
	- var pageUrl = '/privacy_policy';		
	- var headerClass = 'header--black';

block title
	| !{lang.privacyPolicyPageTitle}

block description
	meta(name="description" content=lang.privacyPolicyPageDescription)
	link(rel='canonical' href=lang.websiteDomain + pageUrl)

block og
	meta(name="og:title" content=lang.privacyPolicyPageTitle)
	meta(name="og:description" content=lang.privacyPolicyPageDescription)
	meta(name="og:type" content="website")
	meta(name="og:locale" content=selectedLang)

	meta(name="og:url" content=lang.websiteDomain + pageUrl)
	meta(name="og:image" content=lang.websiteDomain + "/static/img/" + selectedLang + "/og_image.jpg")

	meta(name="twitter:title" content=lang.privacyPolicyPageTitle)
	meta(name="twitter:description" content=lang.privacyPolicyPageDescription)
	meta(name="twitter:card" content="summary_large_image")

	meta(name="twitter:url" content=lang.websiteDomain + pageUrl)
	meta(name="twitter:image" content=lang.websiteDomain + "/static/img/" + selectedLang + "/og_image.jpg")


block content
	main.main
		.terms-info
			.container
				.terms-info__main !{ lang.privacyPolicyContent }